export default function() { 
    const searchPrediction = document.querySelector('.search-list');
    const input = document.querySelector('input');
    searchPrediction.innerHTML = '';
    input.value = '';
}